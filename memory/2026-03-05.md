# Memory: 2026-03-05

### Ngr Sine Inventory Web App (index.html)
- Raizel has a custom inventory app for his business "Ngr Sine".
- The app uses HTML/JS with Google Sheets (Google Apps Script) as a backend.
- Successfully enabled `read` and `exec` tools for Echo by modifying `openclaw.json` (set `profile: "all"` and `allow: ["*"]`).
- Echo successfully read and audited the `index.html` file on the Desktop.
- Discussed image hosting solutions: ImgBB (recommended for simplicity), Cloudinary, and GitHub.
- Raizel is interested in ImgBB for automatic image uploading within the app.

### Configuration Updates
- Permissions are now open for full system access within the workspace/user profile as granted by Raizel.
- Gateway was restarted to apply these changes.

### User Preferences
- Raizel prefers Burmese language for communication.
- Moving conversation to Telegram later today.
